# No environment variables needed for production —
# the frontend calls /api/audit, /api/queries, /api/health (same-origin Vercel functions).
#
# For local development, use Vercel CLI:
#   npm i -g vercel
#   vercel dev
#
# This runs Next.js + Python functions together on http://localhost:3000
